# Free key: https://openweathermap.org/api (sign up, copy API key)
OPENWEATHER_API_KEY=

# Set to 1 to run the ten live OpenWeatherMap tests (`npm test` in lab4). Leave unset for offline-only runs.
OPENWEATHER_RUN_INTEGRATION=
